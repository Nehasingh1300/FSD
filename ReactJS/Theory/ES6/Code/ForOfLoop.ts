// for in will hav eto access every element with index

let colors = ["Red","green","blue"];

for( let index in colors){
	console.log(colors[index]);
}


// for of can easily access all the elements of array
for(let color of colors){
	console.log(color);
}

let letters = "ABC";

for(let letter of letters){
	console.log(letter);
}
